The following is the list of items to test during the API & cloud assessment:

`Status` column can be set for values similar to "Pass", "Fail", "N/A".

`Severity` column can be set for values "0-5" with 0 as the lowest to 5 being the highest potential magnitude of harm or damage if a risk event occurs (how bad).

## 3.3 API & Cloud Integration (IDSA-API)
|Test ID|Test Name|Status|Severity|Notes|
|-|-|-|-|-|
|IDSA-API-001|Insecure Cloud Endpoints|||Intercept Blink App-to-Cloud API calls (Burp Suite).|
|IDSA-API-002|Broken Object Level Auth (BOLA)|||Can one user access another's stream by changing a Device ID?|
